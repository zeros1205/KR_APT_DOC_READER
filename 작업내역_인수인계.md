# PROJECT BALI — 작업 인수인계 문서

> **목적**: 클로드 앱에서 이어서 작업하기 위한 현재까지의 전체 작업 내역 정리  
> **최종 작업일**: 2026-04-19 (2차 세션)  
> **현재 상태**: RAG 파이프라인 연결 완료, run.py 메인 진입점 추가, Step 1~6 전부 통과

---

## 1. 프로젝트 개요

**목표**: 청약홈(한국부동산원) 분양공고 데이터를 자동으로 수집·분석하여  
SEO 최적화된 네이버 블로그 포스팅 HTML을 생성하는 자동화 파이프라인 구축

**발행 방식**: 네이버 블로그 공식 글쓰기 API 미제공 → **HTML 수동 붙여넣기**  
스마트에디터 ONE 우측 상단 `[HTML]` 버튼 → 전체 붙여넣기 → 발행

---

## 2. 프로젝트 경로 및 구조

```
D:\Downloads\재혁\★PROJECT_BALI\아파트분양공고 읽어주기\
│
├── .env                      ← API 키 (실제 키 입력 완료)
├── .env.example              ← 키 템플릿
├── requirements.txt          ← Python 패키지 목록
├── run.py                    ← 메인 실행 스크립트 (API 수집 → RAG → 포스팅 일괄 생성)
├── test_pipeline.py          ← 단계별 테스트 스크립트 (Step 1~6)
├── 전략_마스터플랜.md         ← 전체 전략 문서
├── 작업내역_인수인계.md       ← 이 파일
│
├── .venv/                    ← Python 3.14.3 가상환경 (설치 완료)
│
├── pipeline/
│   ├── config.py             ← API 키·경로·품질기준 설정
│   ├── orchestrator.py       ← 4-에이전트 파이프라인 메인 + run_pipeline_from_doc()
│   ├── html_renderer.py      ← PostData → HTML 렌더링
│   ├── image_finder.py       ← 이미지 수집 (플레이스홀더 + Unsplash)
│   ├── __init__.py
│   └── agents/
│       ├── collector.py      ← 청약홈 API 수집 + CSV 로더 + PDF 파서
│       ├── rag_store.py      ← ChromaDB Vector DB
│       └── __init__.py
│
├── templates/
│   └── blog_template.html    ← 네이버 블로그 호환 HTML 템플릿
│
└── output/
    ├── images/               ← Unsplash 자동 다운로드 이미지
    └── posts/
        ├── 2026-04-19_힐스테이트_판교역/        ← LLM 생성 포스팅
        └── 2026-04-19_힐스테이트_판교역__샘플_/ ← 샘플 데이터 포스팅
```

---

## 3. API 키 현황 (.env)

| API | 상태 | 용도 |
|---|---|---|
| `OPENAI_API_KEY` | ✅ 입력 완료 | 팩트 추출(gpt-4o-mini) + 콘텐츠 생성(gpt-4o) |
| `UNSPLASH_ACCESS_KEY` | ✅ 입력 완료 | 분위기 이미지 자동 수집 |
| `PEXELS_API_KEY` | ✅ 입력 완료 | Unsplash 폴백 이미지 |
| `PUBLIC_DATA_API_KEY` | ✅ 입력 완료 | 청약홈 공공데이터 OpenAPI |

---

## 4. 파이프라인 흐름 (5단계)

```
[Step 1] 데이터 수집 (collector.py)
  ├─ 청약홈 OpenAPI (data.go.kr 서비스ID: 15098547) → 실시간 공고 목록
  ├─ 공고별 주택형별 상세 (getAPTLttotPblancMdl) → 타입/면적/세대수/분양가
  └─ API 키 미설정 시 → 샘플 데이터 자동 폴백

[Step 2] 이미지 수집 (image_finder.py)
  ├─ PLACEHOLDER 슬롯 (4개): 단지 조감도, 59/84타입 평면도, 위치 지도
  │   → 황색 점선 안내 박스 렌더링 (사용자가 직접 교체)
  └─ AUTO 슬롯 (현재 미사용, 필요 시 복원 가능): finance, interior 등

[Step 3] 팩트 추출 Agent 1 (orchestrator.py)
  ├─ GPT-4o-mini → 공고 텍스트에서 JSON 팩트 추출
  └─ Pydantic 검증 + 할루시네이션 방어 (원문에 없는 수치 생성 금지)

[Step 4] 콘텐츠 생성 Agent 2 + 품질 검수 Agent 3·4
  ├─ GPT-4o → Q&A 6개 + SEO 태그 + 입지 별점 생성
  ├─ 품질 점수 70점 미달 시 자동 재생성 (최대 2회)
  └─ CTA 버튼: 현재 비활성 (추후 제휴 링크 등록 후 활성화)

[Step 5] HTML 렌더링 + 로컬 저장 (html_renderer.py)
  ├─ blog_template.html 플레이스홀더 치환
  └─ output/posts/날짜_단지명/ 에 post.html + post_meta.json 저장
```

---

## 5. 테스트 실행 방법

```powershell
# 프로젝트 폴더로 이동
cd "D:\Downloads\재혁\★PROJECT_BALI\아파트분양공고 읽어주기"

# 가상환경 활성화 (이미 설정됨)
.venv\Scripts\Activate.ps1

# 단계별 테스트
python test_pipeline.py --step 1   # 수집기 테스트 (API 불필요)
python test_pipeline.py --step 2   # 이미지 수집 테스트
python test_pipeline.py --step 3   # HTML 렌더링 테스트 (샘플 데이터)
python test_pipeline.py --step 4   # OpenAI 연결 확인
python test_pipeline.py --step 5   # 전체 LLM 파이프라인 실행
python test_pipeline.py --step 6   # RAG 연동 파이프라인 실행 (ChromaDB)

# 메인 실행 스크립트
python run.py --sample             # 샘플 데이터로 전체 테스트 (API 키 불필요)
python run.py --days 7 --limit 3   # 최근 7일 공고 최대 3건 처리
python run.py --sample --open      # 샘플 실행 후 브라우저 자동 열기

# 생성된 포스팅 브라우저 열기
$f=(Get-ChildItem "output\posts" -Filter "post.html" -Recurse | Sort-Object LastWriteTime -Desc | Select -First 1).FullName; Start-Process $f
```

---

## 6. 블로그 템플릿 구조 (blog_template.html)

| 블록 | 섹션명 | 상태 |
|---|---|---|
| BLOCK 1 | 포스팅 헤더 배너 (Navy/Gold 브랜드) | ✅ 완성 |
| BLOCK 2 | 핵심 요약 테이블 + 단지 조감도 플레이스홀더 | ✅ 완성 |
| BLOCK 3 | 타입별 공급 세대수 & 분양가 표 | ✅ 완성 (억/만원 형식) |
| BLOCK 4 | 입지 분석: 교통·학군·생활인프라 (별점 카드 4개) | ✅ 이미지 제거됨 |
| BLOCK 5 | Q&A 6개 블록 | ✅ 완성 (CTA 버튼 비활성) |
| BLOCK 6 | 자금 계획 가이드 (납부 타임라인) | ✅ 이미지 제거됨 |
| BLOCK 7 | ~~입주 준비 체크리스트~~ | ❌ 삭제됨 |
| BLOCK 8 | 세금 정리 표 (취득세·재산세·양도세) | ✅ 완성 |
| BLOCK 9 | 청약 일정 캘린더 (4단계) | ✅ 완성 |
| BLOCK 10 | ~~카카오 채널 구독 배너~~ | ❌ 삭제됨 |
| BLOCK 11 | 면책 고지 & 출처 + SEO 태그 | ✅ 완성 |

**브랜드 컬러**: Navy `#1B3F6B` + Gold `#E8A020`  
**네이버 블로그 호환**: 인라인 스타일 전용, `<script>` 없음

---

## 7. 이미지 처리 전략

| 슬롯 | 타입 | 처리 방식 |
|---|---|---|
| 단지 조감도 | PLACEHOLDER | 황색 점선 안내 박스 → 사용자 직접 삽입 |
| 59/84타입 평면도 | PLACEHOLDER | 황색 점선 안내 박스 → 사용자 직접 삽입 |
| 위치 지도 | PLACEHOLDER | 황색 점선 안내 박스 → 사용자 직접 삽입 |
| 분위기 이미지들 | AUTO (현재 비활성) | Unsplash CC0 자동 수집 가능 (필요 시 재활성화) |

**저작권 원칙**: 단지 이미지(조감도·평면도)는 건설사 자산이므로 자동 수집 불가.  
Unsplash/Pexels는 CC0 라이선스로 상업적 사용 가능 + 출처 자동 표기.

---

## 8. 데이터 소스 상세

### 청약홈 OpenAPI (실시간, 권장)
- **서비스 ID**: 15098547
- **Base URL**: `https://api.odcloud.kr/api/ApplyhomeInfoDetailSvc/v1`
- **주요 엔드포인트**:

| 오퍼레이션 | 설명 |
|---|---|
| `getAPTLttotPblancList` | APT 분양정보 목록 조회 |
| `getAPTLttotPblancDetail` | APT 분양정보 상세 조회 |
| `getAPTLttotPblancMdl` | **주택형별** 상세 (면적·세대수·분양가) |

- **기술문서**: https://www.reb.or.kr/reb/na/ntt/selectNttInfo.do?mi=10251&bbsId=1268&nttSn=79889

### 공공데이터 파일(CSV, 연간)
- **데이터셋 ID**: 15101046
- **한글 컬럼명** 기반 (주택명, 공급위치, 모집공고일 등)
- `load_from_csv(Path)` 함수로 로드

---

## 9. 핵심 설계 결정 사항 (이유 포함)

| 결정 | 이유 |
|---|---|
| 네이버 블로그 API 미사용 | 공식 블로그 글쓰기 API 미제공 확인 (로그인 API 목록에 없음) |
| HTML 수동 붙여넣기 | 스마트에디터 ONE HTML 모드가 가장 안정적 |
| 인라인 스타일 전용 | 네이버 블로그가 `<style>` 태그 무시 |
| 이미지 직접 업로드 | 외부 URL 이미지 네이버 CDN 미러링 불안정 |
| 단지 이미지 PLACEHOLDER | 건설사 저작물 자동 수집 불가 → 법적 리스크 회피 |
| CTA 버튼 비활성 | 제휴 링크 미등록 상태, 추후 활성화 예정 |
| 품질 70점 미달 시 재생성 | 짧은 답변(150자 미만) 방어, 자동 재시도 최대 2회 |
| 분양가 억/만원 표기 | 만원 숫자 그대로 표시(75000) → 가독성 불량 |

---

## 10. 현재 남은 작업 (TODO)

### 즉시 가능
- [x] `pipeline/image_finder_new.py` 삭제 (파일 미존재 확인됨)
- [ ] `output/images/` 폴더 정리 (이전 테스트 이미지)

### 단기 (다음 작업 세션)
- [x] **RAG 파이프라인 연결**: `orchestrator.py`에 `run_pipeline_from_doc()` 추가 완료  
  → ChromaDB 초기화 실패 시 `doc.to_rag_text()` 자동 폴백
- [x] **평면도 슬롯 조건 처리**: 타입 1개 단지는 `floor_plan_84` 슬롯 자동 제거 (orchestrator.py)
- [x] **메인 실행 스크립트**: `run.py` 추가 — `--sample`, `--days`, `--limit`, `--open` 옵션
- [ ] **실제 공고 수집 테스트**: 공공데이터 API 키로 실제 청약홈 공고 1건 수집 → 포스팅 생성  
  → `python run.py --days 7 --limit 1` 실행으로 확인 가능

### 중기
- [ ] **CTA 버튼 활성화**: 핀다(대출), 오늘의집(인테리어), 짐카(이사) 제휴 등록 후  
  `orchestrator.py`의 `QA_CTA_URLS` 복원
- [ ] **카카오 채널 배너 복원**: 채널 개설 후 `blog_template.html` BLOCK 10 재추가
- [ ] **APScheduler 스케줄러**: 매일 오전 9시 자동 실행 설정
- [ ] **ChromaDB RAG 활성화**: `CHROMA_DIR` 경로에 Vector DB 구축 → 팩트 정확도 향상

### 장기 (Phase 1)
- [ ] 실제 네이버 블로그 발행 후 SEO 성과 모니터링
- [ ] 애드포스트 신청 및 광고 매칭 확인
- [ ] 방문자 데이터 기반 Q&A 우선순위 조정

---

## 11. 주요 코드 위치 참조

```python
# 파이프라인 진입점
orchestrator.py → run_pipeline(notice_text: str) -> Path          # 텍스트 직접 입력
orchestrator.py → run_pipeline_from_doc(doc: NoticeDocument) -> Path  # RAG 연동 (권장)

# 데이터 수집
agents/collector.py → CheongYakAPI.get_list() / get_unit_types()
agents/collector.py → get_sample_document()  # 테스트용

# HTML 렌더링
html_renderer.py → BlogHTMLRenderer().render(PostData) -> str
html_renderer.py → UnitType.price_range_str  # 억/만원 포맷

# 이미지
image_finder.py → find_images_for_post(apt_name, output_dir)
image_finder.py → APT_PLACEHOLDER_SLOTS  # 직접 삽입 필요 슬롯
image_finder.py → AUTO_IMAGE_SLOTS       # Unsplash 자동 슬롯 (현재 비활성)

# RAG (구현됨, 미연결)
agents/rag_store.py → ApartmentRAGStore.add_notice(doc)
agents/rag_store.py → ApartmentRAGStore.get_full_context(notice_id)
```

---

## 12. 알려진 이슈

| 이슈 | 증상 | 원인 | 해결책 |
|---|---|---|---|
| 분양가 공고에 없는 경우 | `unit_types` 비어 있음 | API가 분양가 직접 미제공 (별도 조회 필요) | `getAPTLttotPblancMdl` 호출 결과로 보완 |
| 품질 40점 재생성 | 답변 150자 미만 | GPT 응답 길이 불안정 | 자동 재시도로 해결 (2회 내 통과) |
| Unsplash 이미지 임시 비활성 | AUTO 슬롯 현재 미렌더링 | 이미지 구조 단순화 결정 | 필요 시 `image_slot_map`에 슬롯 재추가 |

---

## 13. 환경 정보

| 항목 | 값 |
|---|---|
| OS | Windows |
| Python | 3.14.3 |
| 가상환경 | `.venv` (프로젝트 폴더 내) |
| 주요 패키지 | openai 2.32.0, chromadb 1.5.8, langgraph 1.1.8 |
| 작업 디렉토리 | `D:\Downloads\재혁\★PROJECT_BALI\아파트분양공고 읽어주기` |
