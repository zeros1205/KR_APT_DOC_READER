# 작업 로그 — 2026-04-27 (최근 20시간)

> 프런트페이지·포스트페이지의 데이터·UI·UX 관련 모든 변경 내역

작성 시각: 2026-04-27 (KST)
범위: 직전 약 20시간 (2026-04-27 새벽 ~ 저녁)
주요 산출 PR: **#9 main 정상화** (deploy → main promote), **#10 페이지네이션·UX**

---

## 시간순 커밋 (KST 기준)

### 새벽 (07:25–09:06 KST) — 데이터 파이프라인 재구축

| 시각 | Commit | 내용 |
|---|---|---|
| 07:25 | `849b546` | refactor: rebuild cached post generation and pages deploy config |
| 07:33 | `146d9a5` | chore: 잔여 재생성 post 산출물 추가 |
| 09:04 | `f3efd10` | .gitignore 런타임 산출물 제외 시도 |
| 09:06 | `a6c12bc` | 위 변경 즉시 revert (output 디렉터리 git 보존 정책 유지) |

### 오전 (11:26–12:18 KST) — 프런트페이지 SEO·정책·UI 자산

| 시각 | Commit | 내용 |
|---|---|---|
| 11:26 | `c0928af` | feat: hero "최근 업데이트" 날짜 동적화 (notice_date 최신값 자동 표시) |
| 11:31 | `3e1defd` | feat: og-image.png 추가 (Open Graph 소셜 공유) |
| 11:34 | `7c94135` | feat: favicon 다중 사이즈 (favicon.ico, 16x16, apple-touch-icon) |
| 11:37 | `daa3d61` | feat: privacy.html / terms.html 정책 페이지 작성 |
| 11:38 | `2b469c8` | feat: 푸터에 정책 페이지 링크 추가 |
| 12:12 | `08f71e9` | (main에도 동일 자산 적용) |
| 12:13 | `ed63476` | merge: main → deploy 브랜치 |
| 12:18 | `e800961` | chore: index.html 재생성 (hero 동적 날짜 반영) |

### 오후 (12:58–17:13 KST) — AdSense·정책 정비·promote

| 시각 | Commit | 내용 |
|---|---|---|
| 12:58 | `d1bac50` | terms 페이지: 이미지 라이선스 안내 제거 |
| 13:06 | `03deef3` | feat: Google AdSense `ca-pub-8234120897033274` 메타 태그 |
| 13:10 | `d930124` | chore: index.html AdSense 메타 반영 재생성 |
| 17:13 | `601da75` | docs(privacy): "테마 선택" 항목 삭제 + "(예정)" 표기 제거 |

### 저녁 (18:04 KST) — 페이지네이션 + 지역 탭 dock UX

| 시각 | Commit | 내용 |
|---|---|---|
| 18:04 | `5811f88` | feat: 카드 그리드 페이지네이션 — 5페이지 블록 윈도우 + ‹ › 화살표 |
| 18:04 | `7aaafab` | fix: goPage 스크롤 오프셋이 sticky dock 높이를 동적 반영 |
| 18:04 | `25f80c2` | style: filter-dock margin 상하 0 통일 |
| 18:04 | `df1ef5f` | style: filter-dock padding 12/12 대칭 |
| 18:04 | `5f67329` | fix: goPage breathing room 12 → 32px |
| 18:04 | `5becc5c` | fix: cards-grid margin-top 32px → sticky dock 정상 트리거 + breathing 확보 |

---

## 영역별 변경 요약

### 1. 데이터 파이프라인 (`pipeline/`)

- `849b546` 대규모 재구축 — `orchestrator.py(+777)`, `html_renderer.py(+148)`, `agents/collector.py(+88)`, `agents/pdf_policy.py` 정비, `config.py` 갱신
- `output/posts/` 97개 포스트 재생성 (190+ 파일) — 캐시 기반 빌드 플로우
- `output/data_cache/` 99파일 (notices JSON 캐시)
- `output/processed_notices.json` 갱신

### 2. 프런트페이지 (`templates/front_index_template.html` + `output/index.html`)

| 변경 | Commit |
|---|---|
| 템플릿 슬림화 -145줄 | `849b546` |
| Hero "최근 업데이트" 날짜 동적 표시 | `c0928af` |
| OG·favicon·robots 메타 추가 | `3e1defd`, `7c94135` |
| 푸터 정책 링크 추가 | `2b469c8` |
| AdSense 메타 태그 추가 | `03deef3`, `d930124` |
| 카드 그리드 페이지네이션 5페이지 블록 윈도우 | `5811f88` |
| ‹ › 화살표 네비게이션 | `5811f88` |
| goPage 스크롤 오프셋 동적 계산 | `7aaafab` |
| 지역 탭 dock margin 0/0 대칭 | `25f80c2` |
| 지역 탭 dock padding 12/12 대칭 | `df1ef5f` |
| dock 하단 32px breathing room | `5becc5c` |

### 3. 포스트 페이지 (`templates/blog_template.html` + `output/posts/*/post.html`)

| 변경 | Commit |
|---|---|
| 템플릿 정비 +60줄 | `849b546` |
| HTML 렌더링 로직 정비 (html_renderer.py) | `849b546` |
| 97개 포스트 콘텐츠 재생성 | `849b546`, `146d9a5` |

### 4. 정책·SEO 페이지 (`output/privacy.html`, `output/terms.html`, `templates/`)

| 변경 | Commit |
|---|---|
| privacy.html / terms.html 신규 작성 | `daa3d61` |
| 이미지 라이선스 안내 삭제 (terms) | `d1bac50` |
| 삭제된 "테마 선택" 쿠키 항목 제거 | `601da75` |
| 이메일 "(예정)" 표기 제거 (활성화 반영) | `601da75` |

### 5. 인프라·배포 (`wrangler.jsonc`, `.github/workflows/`)

| 변경 | Commit |
|---|---|
| wrangler.jsonc — name 정정(`kr-apt-doc-reader`) + `pages_build_output_dir` 신문법 | `849b546` |
| 배포 자동화 정비 | `849b546` |

### 6. 운영 인프라 — main 정상화 + Cloudflare Pages 전환

| 작업 | 결과 |
|---|---|
| Cloudflare Pages production branch | `claude/codex-review-1777248690` → **`main`** |
| main 트리/콘텐츠 | deploy 브랜치 103커밋 + privacy 정정 1커밋을 fast-forward로 흡수 |
| `claude/codex-review-1777248690` | **비상 백업용 브랜치로 보존** (push 금지 합의) |
| 임시 브랜치 정리 | `temp/promote-deploy-to-main`, `claude/card-grid-pagination-CBocp`, `codex/push-main-test`, `claude/review-claude-md-nTSPo` 삭제 |

---

## 사용자 가시 영향 (apt-note.com에 라이브 반영)

### 프런트페이지에서 즉시 보이는 변경

1. Hero "최근 업데이트" 날짜 — 하드코딩 제거, 가장 최신 분양공고의 notice_date로 자동 갱신
2. Open Graph 이미지 — 카카오톡/Slack 등 공유 시 썸네일 표시
3. Favicon — 브라우저 탭 아이콘 정상 표시
4. 푸터 정책 링크 — 개인정보·이용약관 접근 경로 확보
5. 5페이지 블록 페이지네이션 — 페이지 ≤5: 그대로, >5: 5개씩 묶음 + ‹ › 이동
6. 모바일 페이지 이동 시 카드 가림 해결 — sticky 지역 탭 높이만큼 자동 오프셋
7. 지역 탭 영역 마진/패딩 대칭 — 시각적 정합성 향상
8. 카드 그리드 상단 32px 호흡 공간 — 빽빽하지 않은 레이아웃

### 정책 페이지의 정확성

1. `/privacy.html`에서 더 이상 존재하지 않는 "테마 선택" 기능 안내 제거
2. 이메일 `contact@apt-note.com`이 실제 운영 중임을 반영

### 백엔드 안정성

1. 분양 포스트 97개 전면 재생성 — 데이터 일관성
2. 캐시 기반 빌드 플로우 — 재처리 효율 향상
3. wrangler 신문법 적용 — Cloudflare Pages 빌드 규약 준수

---

## 통계

- 총 commit: 22개 (이번 세션 6 + 직전 deploy 브랜치 16)
- 변경 파일: 308개 (이번 promote 기준)
- 변경 라인: +124,076 / −166
- 머지된 PR: 2건 (#9 main 정상화, #10 페이지네이션·UX)

---

## 핵심 기술 디테일 — 페이지네이션 + 스크롤 (이번 세션 핵심)

### 페이지네이션 동작

```js
// pipeline/index_renderer.py 내부 INDEX_RUNTIME_SCRIPT 일부
var BLOCK = 5;
var block = Math.ceil(_currentPage / BLOCK);
var totalBlocks = Math.ceil(pages / BLOCK);
var start = (block - 1) * BLOCK + 1;
var end = Math.min(block * BLOCK, pages);

if (block > 1) {
  // ‹ 이전 5페이지: 이전 블록의 마지막 페이지로 이동
}
for (var i = start; i <= end; i++) { ... }
if (block < totalBlocks) {
  // › 다음 5페이지: 다음 블록의 첫 페이지로 이동
}
```

### goPage 스크롤 오프셋 계산

```js
var dock = document.getElementById('filter-dock');
var stickyTop = dock ? (parseFloat(getComputedStyle(dock).top) || 0) : 0;
var dockH = dock ? dock.offsetHeight : 0;
var offset = stickyTop + dockH + 32;  // 32px breathing room
if (window.scrollY > grid.offsetTop - offset - 40) {
  window.scrollTo({ top: grid.offsetTop - offset, behavior: 'smooth' });
}
```

### CSS 정합성

- `#filter-dock`: `margin: 0 -24px;`, `padding: 12px 24px;` (상하 대칭)
- `.cards-grid`: `margin-top: 32px;` (sticky 임계점 보장 + 시각 breathing)

---

## 참고 — 산출물 소스/배포 동기화 원칙

- 소스 수정: `pipeline/index_renderer.py`의 `INDEX_RUNTIME_SCRIPT` (Python 문자열)
- 빌드 산출물: `output/index.html`이 매 포스트 생성 시 자동 재주입됨
- 즉시 배포가 필요한 경우 두 파일 동시 surgical 편집 → diff 검증 → commit
- UI Freeze 가드(`UI_FREEZE_MANIFEST.json`)는 advisory (CI 미강제)
