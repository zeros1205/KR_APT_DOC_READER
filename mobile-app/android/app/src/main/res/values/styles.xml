<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. 웹뷰(Capacitor) 앱이라 네이티브 액션바를 쓰지 않는다.
         DarkActionBar 기반이면 앱 라벨("청약노트")+아이콘 액션바가 화면 상단에
         상주하므로 NoActionBar 기반으로 둔다. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@drawable/splash</item>
        <!-- 스플래시 종료 후 적용할 런타임 테마. 누락 시 액션바가 있는 테마로 남아
             앱 라벨("청약노트")+아이콘이 모든 페이지 헤더 위에 상주하는 회귀가 발생. -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>