name: codex-페이지 생성하기

on:
  workflow_dispatch:
    inputs:
      limit:
        description: "최대 생성 건수, 0은 제한 없음"
        required: true
        default: "1"
      notice_id:
        description: "특정 공고번호, 비우면 캐시 순서대로 생성"
        required: false
        default: ""
      include_manual:
        description: "manual_review 공고 포함 여부"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      include_processed:
        description: "이미 생성된 공고 재생성 여부"
        required: true
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

jobs:
  generate-pages:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write

    env:
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      PUBLIC_DATA_API_KEY: ${{ secrets.PUBLIC_DATA_API_KEY }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Create .env
        run: |
          cat > .env <<EOF
          GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}
          PUBLIC_DATA_API_KEY=${{ secrets.PUBLIC_DATA_API_KEY }}
          EOF

      - name: Verify frozen UI baseline
        run: python check_ui_freeze.py

      - name: Generate posts and front page
        run: |
          LIMIT="${{ github.event.inputs.limit }}"
          NOTICE_ID="${{ github.event.inputs.notice_id }}"
          INCLUDE_MANUAL="${{ github.event.inputs.include_manual }}"
          INCLUDE_PROCESSED="${{ github.event.inputs.include_processed }}"

          CMD="python generate_posts_from_cache.py --limit $LIMIT"

          if [ -n "$NOTICE_ID" ]; then
            CMD="$CMD --notice-id $NOTICE_ID"
          fi
          if [ "$INCLUDE_MANUAL" = "true" ]; then
            CMD="$CMD --include-manual"
          fi
          if [ "$INCLUDE_PROCESSED" = "true" ]; then
            CMD="$CMD --include-processed"
          fi

          echo "$CMD"
          eval "$CMD"

      - name: Verify UI files were not modified
        run: |
          python check_ui_freeze.py
          git diff --exit-code -- \
            UI_FREEZE_MANIFEST.json \
            templates/blog_template.html \
            templates/front_index_template.html \
            pipeline/html_renderer.py \
            pipeline/index_renderer.py \
            pipeline/shared_ui.py \
            pipeline/themes.py

      - name: Commit generated pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add output/posts output/index.html output/sitemap.xml output/robots.txt output/processed_notices.json

          if ! git diff --cached --quiet; then
            git commit -m "chore: generate pages [$(date +%Y-%m-%d)]"
            git push
          else
            echo "No generated page changes."
          fi
